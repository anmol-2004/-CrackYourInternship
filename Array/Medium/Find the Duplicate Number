// Leetcode 287

class Solution {
public:
    int findDuplicate(vector<int>& nums) {
        vector<int> result=nums;
        
        while(result[0]!=result[result[0]]){
            swap(result[0], result[result[0]]);
        }
        return result[0];
    }
};
